
const QuestionBody = ({
  title
}: any) => {
	return (
		<div className="flex flex-col items-start gap-[5px] p-content bg-colorBackgroundComponents shadow-lg rounded-lg">
      title
		</div>
	);
};

export default QuestionBody;
